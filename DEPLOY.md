# 部署指南

本指南将帮助你将这个博客项目部署到 GitHub Pages。

## 前置条件

1. 一个 GitHub 账号
2. 一个 GitHub 仓库
3. GitHub Pages 功能已启用

## 步骤

### 1. 创建 GitHub 仓库

1. 登录 GitHub 并创建新仓库
2. 仓库名称可以命名为：`vite-vue3-blog` 或其他你喜欢的名称
3. 确保仓库是 **Public** 的（GitHub Pages 免费版只支持公开仓库）

### 2. 上传代码到仓库

```bash
# 初始化 git 仓库
git init

# 添加远程仓库地址（替换为你的仓库地址）
git remote add origin https://github.com/yourusername/vite-vue3-blog.git

# 添加所有文件
git add .

# 提交代码
git commit -m "初始化博客项目"

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 3. 配置 GitHub Pages

1. 进入仓库的 **Settings** 页面
2. 滚动到 **Pages** 章节
3. 在 **Source** 下拉菜单中选择 **GitHub Actions**
4. 保存设置

### 4. 自动部署

推送代码到 `main` 分支后，GitHub Actions 会自动：

1. 检测到代码变更
2. 安装依赖
3. 构建项目
4. 部署到 GitHub Pages

你可以在仓库的 **Actions** 页面查看构建状态。

### 5. 访问你的博客

部署完成后，你的博客可以通过以下地址访问：

```
https://yourusername.github.io/vite-vue3-blog/
```

## 手动部署

如果你想手动部署，可以运行以下命令：

### VitePress 文档部署
```bash
npm run docs:build
# 将 dist-docs/ 目录上传到你的托管服务
```

### Vue App 部署
```bash
npm run build
# 将 dist/ 目录上传到你的托管服务
```

## 自定义域名（可选）

如果你有自己的域名，可以在仓库根目录添加 `CNAME` 文件：

```bash
echo "yourdomain.com" > CNAME
```

然后在你的域名提供商处配置 DNS：

```
类型: CNAME
名称: www
值: yourusername.github.io
```

## 故障排除

### 常见问题

1. **构建失败**
   - 检查 Actions 页面的错误日志
   - 确保 `package.json` 中的依赖版本兼容
   - 检查 TypeScript 编译错误

2. **页面空白**
   - 检查 Vite 配置中的 `base` 路径
   - 确保 GitHub Pages 设置正确
   - 检查控制台是否有 JavaScript 错误

3. **路由404错误**
   - 确保使用 `cleanUrls: true` 配置
   - 或使用 `hash` 路由模式

### 调试技巧

1. **本地预览**
   ```bash
   # 本地预览 Vue App
   npm run build && npm run preview
   
   # 本地预览 VitePress
   npm run docs:build && npm run docs:serve
   ```

2. **检查构建输出**
   ```bash
   npm run build -- --mode analyze
   ```

## 最佳实践

1. **定期更新依赖**
   ```bash
   npm update
   ```

2. **使用分支开发**
   ```bash
   # 创建特性分支
   git checkout -b feature/new-article
   
   # 完成后合并到 main
   git checkout main
   git merge feature/new-article
   ```

3. **提交信息规范**
   ```
   feat: 添加新文章
   fix: 修复导航组件问题
   docs: 更新 README
   style: 优化样式
   ```

## 进一步优化

### 性能优化
- 启用 Gzip 压缩
- 使用 CDN 加速
- 图片懒加载
- 代码分割

### SEO 优化
- 添加 meta 标签
- 使用语义化 HTML
- 生成 sitemap
- 添加结构化数据

### 监控和分析
- 集成 Google Analytics
- 添加错误监控
- 性能监控

祝你部署成功！🎉